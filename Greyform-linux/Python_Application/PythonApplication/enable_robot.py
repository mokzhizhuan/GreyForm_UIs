# import rospy


from PyQt5 import QtCore, QtWidgets, QtGui
from PyQt5.QtWidgets import *
from PyQt5.QtCore import *
from PyQt5.QtWidgets import *
from PyQt5.QtGui import *


# enable the robot bim interpretor
class EnableRobotInterpreter(object):
    """def __init__(self):"""

    """rospy.init_node('robot_controller')

        # Subscribe to velocity command topic
        rospy.Subscriber('cmd_vel', Twist, velocity_callback)

        # Spin to keep the node alive
        rospy.spin()"""

    """def velocity_callback(msg):
    # Process velocity command
    # For demonstration, let's just print the received commands
        rospy.loginfo("Received velocity command: %s", msg)"""
    """try:
            main()
        except rospy.ROSInterruptException:
        pass"""
